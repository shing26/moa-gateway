"""Optional orchestration adapters.

Nothing in the default runtime imports this package. Everything here exists to
answer one question with code instead of prose: *given the same collaborators,
what does a mainstream agent framework change?*

``graph.py`` requires the optional ``langgraph`` extra::

    uv sync --extra langgraph
"""
