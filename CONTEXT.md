# MoA Gateway

LLM 调用治理层的领域语言。这个 context 存在的理由：本项目治理的对象是**模型输出**，
不是操作动作；它的价值主张是每个断言都能被独立质证。以下术语用来把这条主张说准确——
尤其用来区分那些看起来同义、实际指向不同概念的词。

## 治理对象与判定

**治理对象 (Governance Subject)**:
进入本项目闸门的东西——**模型输出**。
_Avoid_: 操作、变更、工单（那些是"操作审批"的对象，本项目不治理）

**护栏判定 (Guard Verdict)**:
对一次输出的三态裁决：放行 / 拦截 / 转人工。不是二元的放行与拒绝，中间那一档是本项目的关键。
_Avoid_: 审核、过滤

**挂起 (Suspension)**:
输出已被扣住、等待人工裁决的中间状态。挂起中的输出既未送达、也未丢弃。
_Avoid_: 待办、排队

## 任务执行

**工具轮次 (Tool Round)**:
一次工具调用及其观察结果构成的循环单元。上限是**有界**的，不是直到模型自己满意为止。
_Avoid_: ReAct 步数、迭代次数（同义；本项目统一用"工具轮次"，上限只有一个来源）

**失败升级 (Escalation)**:
自动处理重试耗尽后转入人工。它是**转人工的一种来源**，与"策略命中而转人工"共用同一个闭环，
但起因不同——一个是"没做成"，一个是"不该直接做"。
_Avoid_: 报错、兜底

## 人工介入

**审批 (Approval)**:
需要人工做出放行/拒绝裁决的介入。它的判据是：末端有一次**可留痕的决策**。
_Avoid_: 复核、确认

**通知 (Notification)**:
只告知、不含裁决动作的介入。与"审批"外形相似（都可能是一张卡片）但**不是审批**——
渲染成审批按钮会承诺一个点了必然失效的动作。
_Avoid_: 审批（当实际没有决策动作时）

**审批人 (Approver)**:
被允许做出裁决的人。"能不能批"由白名单回答；"该不该由这个角色批"是另一个问题。
_Avoid_: 用户、操作者

## 路由与降级

**路由层级 (Routing Level)**:
意图判定来自哪一层：正则 / 微模型 / 路由模型 / 默认。
_Avoid_: 兜底层（"默认"表示**没有取得判定**，与"判定结果恰好是默认意图"是两件事）

**降级 (Degradation)**:
上游不可用时自动切到更弱但可用的档位，而不是拒绝服务。
_Avoid_: 熔断（熔断是降级的一种触发原因，不是同义词）

## 闭环

**闭环 (Closed Loop)**:
判据不是"代码绕回来了"，而是**回路末端有没有人消费产出**。

**治理闭环**:
从拦截或转人工，到人工裁决，再到留痕的回路。末端是**人**。

**改进闭环**:
从评测产出，到某个决策点消费该结论并据此改变系统的回路。末端是**消费结论的决策点**。
_Avoid_: 评测（评测只是产出，不构成闭环）

**业务闭环**:
从真实需求，到真实用户消费产出的回路。末端是**真实用户**。
_Avoid_: 使用、上线

**落盘字段 (Persisted Field)**:
审计条目中**真正被写入存储**的字段——与"只在运行期内存里存在"的字段相对。
这两者的差别曾经让文档替实现担保过。
_Avoid_: 审计字段（那是上位概念，不区分是否落盘）
